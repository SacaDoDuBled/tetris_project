void creer_Tab(char *grille[HEIGHT][WIDTH]);

void afficheTab(char *grille[HEIGHT][WIDTH]);

void ecrire_piece(char *grille[HEIGHT][WIDTH], int p, int c, int h);

int hauteur_max(char *grille[HEIGHT][WIDTH], int a, int b);

int supprime_ligne(char *grille[HEIGHT][WIDTH]);